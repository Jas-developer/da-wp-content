name: Publish Website

on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: 🚀 Deploy Website Every Commit
    runs-on: ubuntu-latest
    
    steps:
      - name: 🚚 Get Latest Code
        uses: actions/checkout@v3

      - name: 📂 Deploy via SCP
        uses: appleboy/scp-action@master
        with:
          host: 217.196.55.174
          username: u222328741
          key: ${{ secrets.SSH_KEY }}
          port: 65002
          source: "wp-content/mu-plugins/*"
          target: "/home/u222328741/domains/darkslategray-kingfisher-643992.hostingersite.com/public_html/wp-content/mu-plugins"
          strip_components: 1